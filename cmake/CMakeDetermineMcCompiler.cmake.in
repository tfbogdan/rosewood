cmake_minimum_required(VERSION 3.10)


if (NOT CMAKE_Mc_COMPILER)
  set(CMAKE_Mc_COMPILER ${CMAKE_CURRENT_LIST_DIR}/../../../bin/mc CACHE INTERNAL "Use mc from build directory")
endif(NOT CMAKE_Mc_COMPILER)
configure_file(${CMAKE_CURRENT_LIST_DIR}/CMakeMcCompiler.cmake.in CMakeFiles/${CMAKE_VERSION}/CMakeMcCompiler.cmake @ONLY)
set(CMAKE_Mc_COMPILER_ENV_VAR "mc")
#if (TARGET mc)
#  set(CMAKE_MC_COMPILER ${CMAKE_CURRENT_LIST_DIR}/../../../bin/mc CACHE INTERNAL "Use mc from build directory")
#else()
#  find_program(mcBinary NAMES mc)
#  if (not MC)
#    message(FATAL_ERROR "Cannot find a metadata compiler.")
#  endif()
#  set(CMAKE_MC_COMPILER mc CACHE INTERNAL "Use mc from build directory")
#endif()

#endif()

